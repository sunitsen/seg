import React from 'react';
import HeroContent from './HeroContent';
import HeroSlider from './HeroSlider';

const Hero = () => {
  return (
    <div className="max-w-screen-7xl w-full mx-auto custom-bg-primary flex flex-col md:flex-row flex-wrap md:justify-start justify-center items-center min-h-screen p-4">
      <div className="w-full md:w-2/5 flex items-center justify-center p-4 z-10">
        <HeroContent />
      </div>

      <div className="w-full md:w-3/5 flex items-center justify-center p-4 z-0">
        <HeroSlider />
      </div>
    </div>
  );
};

export default Hero;